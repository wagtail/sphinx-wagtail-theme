.. highlight:: shell

============
Installation
============


Stable release
==============

To install Sphinx Wagtail theme, run this command in your terminal:

.. code-block:: console

    $ pip install sphinx-wagtail-theme

This is the preferred method to install Sphinx Wagtail theme, as it will always
install the most recent stable release.
